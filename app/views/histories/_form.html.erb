<%= form_with(model: history) do |form| %>
  <% if history.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(history.errors.count, "error") %> prohibited this history from being saved:</h2>

      <ul>
        <% history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :release_train_id, style: "display: block" %>
    <%= form.text_field :release_train_id %>
  </div>

  <div>
    <%= form.label :version, style: "display: block" %>
    <%= form.number_field :version %>
  </div>

  <div>
    <%= form.label :run_result, style: "display: block" %>
    <%= form.text_field :run_result %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
