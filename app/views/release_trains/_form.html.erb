<%= form_with(model: release_train) do |form| %>
  <% if release_train.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(release_train.errors.count, "error") %> prohibited this release_train from being saved:</h2>

      <ul>
        <% release_train.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :engineering_manager, style: "display: block" %>
    <%= form.text_field :engineering_manager %>
  </div>

  <div>
    <%= form.label :backend, style: "display: block" %>
    <%= form.text_field :backend %>
  </div>

  <div>
    <%= form.label :rt_date, style: "display: block" %>
    <%= form.date_field :rt_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
